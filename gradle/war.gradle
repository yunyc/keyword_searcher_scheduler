apply plugin: "war"

bootWar {
    mainClass = "com.example.scheduler.SchedulerApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
